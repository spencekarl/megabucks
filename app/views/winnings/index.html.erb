<!DOCTYPE html>
<body>
    <article>
      <h1><%= @announcement %></h1>
      <h2 class="animated infinite tada"><%= "$#{@winnings}" if @i_won %></h2>
    </article>

<div style="position: absolute;  overflow: hidden;top: 0; left: 0; z-index: -99; width: 100%; height: 100%">
<iframe id="video" height="100%" width="100%"
  src="<%= @video_url %>"
  frameborder="0">
</iframe>
</div>
</body>
</html>
